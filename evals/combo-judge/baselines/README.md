# Baseline Snapshots

This directory contains committed baseline snapshots used for regression comparison.

Each snapshot captures the model output for a fixed set of queries, enabling detection of prompt regressions and scoring drift across iterations.
